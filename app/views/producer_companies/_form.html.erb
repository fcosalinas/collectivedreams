<%= form_for(@producer_company) do |f| %>
  <% if @producer_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@producer_company.errors.count, "error") %> prohibited this producer_company from being saved:</h2>

      <ul>
      <% @producer_company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :phone %><br>
    <%= f.text_field :phone %>
  </div>
  <% @users = User.all %>
  <div class="field">
    <%= f.label :user_id, "Dueño" %><br>
    <%= f.select :user_id, options_from_collection_for_select(@users, "id", "full_name") %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
